# Non-secret config
POSTGRES_USER=lynx
POSTGRES_DB=lynx_dashboard
POSTGRES_PASSWORD=lynx
RUST_LOG=info

# Secrets go in secrets/*.txt (never committed)
# secrets/internal_api_token.txt  — shared bearer token (UI→backend, internal network only)
#   generate: openssl rand -hex 32 > secrets/internal_api_token.txt
